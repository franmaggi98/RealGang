import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.RealGang.app',
  appName: 'RealGangTournaments',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
